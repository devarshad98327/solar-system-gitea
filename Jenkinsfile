pipeline {
    agent any

    stages {
        stage('Check Node and npm Version') {
            steps {
                bat 'node -v'      // Prints Node.js version
                bat 'npm -v'       // Prints npm version
            }
        }
    }
}
